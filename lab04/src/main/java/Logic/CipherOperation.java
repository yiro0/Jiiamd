package Logic;

/**
 * An enum representing the operation to be performed by a cipher.
 *
 * @author Bartek
 * @version 3.1
 */

public enum CipherOperation {
    /** The operation to encode text. */
    ENCODE,
    /** The operation to decode text. */
    DECODE
}
